clc
clear

ZL=50;          %传输线负载阻抗

Er=5.8;         %介电常数
h=20*0.0254*0.001;    %走线到参考平面的距离
w=20*0.0254*0.001;    %线宽
t=0.7*0.0254*0.001;     %铜皮厚度
%Z0=87/(sqrt(Er+1.41)*log(5.98*H/(0.8*W+T)));

if w/h<=1
    Z0a=59.952*log(8*h/w+w/(4*h));
else 
    Zoa=119.904*pi/(w/h+2.42-0.44*h/w+(1-h/w)^6);
end

if w/h<=1
    q=1/2+1/2*((1+12*h/w)^(-1/2)+0.041*(1-w/h)^2);
else
    q=1/2+1/2*(1+12*h/w)^(-1/2);
end

Ee=1+q*(Er-1);

Z0=

% S11=(ZL-Z0)/(ZL+Z0)
% dbS11=20*log10(S11)






